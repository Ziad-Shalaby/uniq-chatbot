# UniQ Streamlit App - Requirements
# Install: pip install -r requirements.txt

streamlit>=1.35.0
transformers>=4.44.0
accelerate>=0.34.0
sentence-transformers>=3.1.0
chromadb>=0.5.5
pdfplumber>=0.11.0
torch>=2.1.0           # Install separately for your CUDA version if needed
bitsandbytes>=0.43.0   # For 4-bit quantization (GPU only, skip on CPU-only)
sentencepiece>=0.2.0
